<form method="post" action="{{ path('example_delete', {'id': example.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ example.id) }}">
    <button class="btn">Delete</button>
</form>
