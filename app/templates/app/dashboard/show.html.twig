{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
        <tr>
            <td><h3>{{ post.title }}</h3></td>
        </tr>
        <hr>
        <tr>
            <td><p>{{ post.text_content ?? 'Brak treści'}}</p></td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('dashboard_index') }}">Back to list</a>

    <a href="{{ path('dashboard_edit', {'id': post.id}) }}">Edit</a>

    {{ include('app/_delete_form.html.twig', {
        'item_id': post.id,
    }) }}
{% endblock %}
