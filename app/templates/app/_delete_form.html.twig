<form method="post" action="{{ path('dashboard_delete', {'id': item_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_id) }}">
    <button class="btn">Delete</button>
</form>
